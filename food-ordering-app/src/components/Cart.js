const Cart = () => {
    return (
        <div className="page">
            <h1>Cart</h1>
            <p>Your cart is empty. Add items from the home page to get started.</p>
        </div>
    );
};

export default Cart;
